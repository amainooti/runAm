// const btn= document.querySelector(".btn");


// const carouselSlide = document.querySelector('.categories');
// const carouselImages = document.querySelectorAll('.categories div');

// // buttons
// const prevBtn = document.querySelector('.prev-btn');
// const nextBtn = document.querySelector('.next-btn');

// // counter
// let counter = 0;
// const slideWidth = carouselImages[0].clientWidth * 8;

// carouselSlide.style.transform = 'translateX(' + (-slideWidth * counter) + 'px)';

// // button listeners
// nextBtn.addEventListener('click', () => {
//   if (counter >= carouselImages.length / 8 - 1) return;
//   carouselSlide.style.transition = 'transform 0.6s ease-in-out';
//   counter++;
//   carouselSlide.style.transform = 'translateX(' + (-slideWidth * counter) + 'px)';
// });

// prevBtn.addEventListener('click', () => {
//   if (counter <= 0) return;
//   carouselSlide.style.transition = 'transform 0.6s ease-in-out';
//   counter--;
//   carouselSlide.style.transform = 'translateX(' + (-slideWidth * counter) + 'px)';
// });
